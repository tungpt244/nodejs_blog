<div class="mt-4">
    <form method="GET" action="">
    {{#each test}}
    <div class="user">{{test.name}}</div>
    {{/each}}
    <div class="mb-3">
        <label for="search-input" class="form-label">Tìm kiếm dữ liệu</label>
        <input type="text" name="q" class="form-control" id="search-input" placeholder="Nhập vào từ khóa...">
     </div>

    <div class="mb-3">
        <label for="search-input" class="form-label">Tìm kiếm dữ liệu</label>
        <select name="gender" class="form-control" id="gender-input" >
            <option value="">-- Chọn giới tính --</option>
            <option value="male">Nam</option>
            <option value="female">Nữ</option>
        </select>
    </div>
    
    <button type="submit" class="btn btn-primary">Tìm kiếm</button>
    </form>
</div>